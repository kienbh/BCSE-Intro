# REVIEW BCSE Intro (sv08) — 2026-08-29 đêm

> **Vai trò review:** design reviewer + product critic — rà soát thiết kế LẦN ĐẦU có hệ thống cho cổng chung sv08.bcse-vju.com.
> **Người rà:** Claude Code (phiên 29/8 đêm). **Không sửa code, không deploy** — chỉ report này.
> **Tài liệu chị em:** `docs/DESIGN-AUDIT-LIGHT-DARK-2026-08.md` (rà riêng light/dark cùng ngày — report này KHÔNG lặp lại chi tiết, chỉ dẫn chiếu và bổ sung số đo tương phản).

---

## 0. TL;DR + bảng điểm

**Một câu:** sv08 là một brochure dark-mode đẹp và có nền dữ liệu 2 khung 135/152 làm nghiêm túc, nhưng **đang nói dối người dùng ở 4 chỗ trọng yếu** — mã ngành tuyển sinh 2026 sai, card E-Service lộ trên trang chủ trái quyết định 27/8, card sv02/sv15 mô tả app không còn tồn tại ở URL đó, và số liệu việc làm không thể kiểm chứng — cộng thêm logo 5.4 MB tải trên mọi trang và light mode vỡ hệ thống. Vai trò "front door" mới đạt mức "cửa có biển hiệu đẹp"; chưa đạt mức "đẩy đúng người đến đúng app".

| # | Hạng mục | Điểm /10 | Nhận xét ngắn |
|---|---|---|---|
| 1 | Vai trò front-door | **5** | Đường đi ≤2 click cho cả 4 persona ĐỀU CÓ, nhưng teaser trang chủ chọn sai app (lộ sv04 staff-only, sv02 mô tả lỗi thời), K2026 bấm Đăng nhập hiện chưa vào được (bcse-id chưa seed) |
| 2 | IA & nội dung | **5** | Khung 135/152 trên /curriculum làm tốt; nhưng mã ngành tuyển sinh 2026 vẫn CHỈ ghi 7480204QTD (🔴 đúng trường hợp cảnh báo), 3 cặp số liệu tự mâu thuẫn, 2 card app trỏ nhầm thực thể |
| 3 | Thiết kế thị giác & hệ thống | **5** | Dark mode identity ổn (đáng 7); light mode "giả lập bằng blocklist" vỡ hệ thống (đã có audit riêng); 6 họ font, 2 giọng display xung đột |
| 4 | A11y + responsive | **4** | Ngay ở DARK mode: chữ mô tả 12px slate-500 = 4.24:1 (fail AA), slate-600 = 2.66:1, chip active trắng/sky-500 = 2.77:1; subpage không có h1; carousel không pause được trên touch; không prefers-reduced-motion |
| 5 | Kỹ thuật (SEO/perf/i18n) | **4.5** | Logo 5,567,981 bytes live; 0 thẻ OG; sitemap 404; /research không có cả title; EN/JA vô hình với crawler (i18n client-side); file BCSE.docx 1.3MB nằm công khai không ai tham chiếu |

Đếm findings: **9 🔴 · 14 🟡 · 8 🟢** (chi tiết §3).

---

## 1. Phạm vi & phương pháp

- **Code:** đọc trực tiếp toàn bộ `app/*` (7 route), `components/*` (22 file), `data/*` (13 file), `lib/*`, `globals.css`, `tailwind.config.ts`, `next.config.mjs` tại `d:\files\bcse-intro`.
- **Chiến lược đối chiếu:** `docs/STRATEGY-2026.md` (P3 — "SV08 hiện là catalog chưa phải cửa"), `docs/MASTER-PLAN-2026.md` (N1/N2), `docs/EXECUTION-FALL-2026.md` (D6/D7/D8 — các quyết định đã chốt), `docs/DESIGN-AUDIT-LIGHT-DARK-2026-08.md`.
- **Registry đối chiếu:** `d:\files\bcse-id\src\data\apps.ts` (chỉ đọc) — whitelist dashboard sau login.
- **Render thật:** fetch công khai `https://sv08.bcse-vju.com/` + `/services/` + `/contact/` + `/curriculum/` (29/8 đêm, HTTP 200) — mọi finding nội dung đều verify có mặt trên bản LIVE, không chỉ trong repo.
- **Đo lường:** tính tương phản WCAG bằng script (relative luminance) cho các cặp màu thật trong code; đo size asset live bằng curl.
- **Giới hạn:** không chạy Lighthouse thật, không login thử SSO, không mở nội dung `BCSE.docx`; phần dashboard sau login đánh giá qua code `apps.ts` (không có render).

---

## 2. Hiện trạng thật (route map + section map, mã ↔ render khớp)

### 2.1 Route map

| Route | Nội dung | h1 | metadata riêng |
|---|---|---|---|
| `/` | 12 section (dưới) | ✅ "BCSE" (hero) | title+description gốc (`app/layout.tsx:7-11`) |
| `/curriculum` | Toggle 2 khung, **mặc định 152** (`app/curriculum/Content.tsx:34`); khung 135 đầy đủ: 7 khối TC, 23 TC cốt lõi, 5 mô đun (3 cửa ngõ), 3 dự án, 2 phương án TN, kế hoạch 8 kỳ | ❌ | title ✅ |
| `/research` | 3 lab + 6 hướng NC + publications + blog + 7 NCKH SV; i18n đầy đủ nhất site | ❌ | **❌ không có cả title** (`app/research/page.tsx:1` là `'use client'` nên không export được metadata) |
| `/facilities` `/faculty` | brochure | ❌ | title ✅ |
| `/services` | Chỉ dịch vụ audience `student` (`app/services/Content.tsx:28`); view "Dịch vụ" (mặc định) + "🗺 Bản đồ 4 năm"; search + filter | ❌ | title ✅ |
| `/contact` | Liên hệ + khối "Tuyển sinh 2026" (mã trường/mã ngành/chỉ tiêu/tổ hợp/phương thức) | ❌ | title ✅ |

### 2.2 Section map trang chủ (thứ tự trong `app/page.tsx:17-28`)

Hero (carousel 7 ảnh) → Triết lý → Stats (6 số đếm) → 5 Định hướng → Curriculum preview (**chỉ khung 152** — `CurriculumPreview.tsx:3`) → Cơ sở vật chất → Nghiên cứu → Giảng viên → **BCSE Portal teaser 6 card** → Dự án SV → Cơ hội việc làm → CTA tuyển sinh.

### 2.3 Navigation & cửa sau login

- Nav: 7 mục + nút **Đăng nhập** → `https://id.bcse-vju.com` (`Navbar.tsx:62,105` — verify live 2 chỗ desktop+mobile, đúng hướng B của D8) + nút **Tuyển sinh 2026** → `vju.ac.vn/ttts2026/` (`data/navigation.ts:20`).
- Dashboard bcse-id (`d:\files\bcse-id\src\data\apps.ts:214-223`): whitelist hiện có **4 id** (sv14, sv18, sv09, **sv02-compass** — nhiều hơn "3 app" trong bối cảnh đã biết). SV nhìn thấy **3 card**: sv14 Hardware Lab, sv09 Career, sv02 Compass; **sv18 Tracker gate đúng** qua `visibleTo: ['lecturer','director','staff']` (`apps.ts:158`) + hàm `visibleApps()` (`apps.ts:226-233`) → SV KHÔNG thấy sv18. Câu hỏi "sv18 staff-only mà hiện với SV thì sao" — **không xảy ra ở dashboard**; nhưng xảy ra ở chính sv08 (finding F10).

### 2.4 Walkthrough 4 persona (≤2 click?)

| Persona | Đường đi | Đạt? | Vấp phải |
|---|---|---|---|
| (a) HS THPT cân nhắc đăng ký | Nav "Tuyển sinh 2026" → trang VJU (1 click); hoặc /contact (1 click) | ✅ cơ học | **Mã ngành hiển thị là mã cũ 7480204QTD** (F1); curriculum mặc định khung cũ; số liệu việc làm không nguồn (F8) làm giảm độ tin |
| (b) Tân SV K2026 | "Đăng nhập" → id.bcse-vju.com (1 click) | ⚠ | K2026 **chưa seed / 0 tài khoản** trên bcse-id (memory 28/8) → click xong chết cửa; sv08 không có khối "Tân SV bắt đầu tại đây" hướng dẫn đăng nhập lần đầu (F9) |
| (c) SV năm 2–4 cần app trong kỳ | "Dịch vụ" → card app (2 click) | ✅ | Card sv02 mô tả app không tồn tại (F3), card Guild trỏ nhầm BCSE World (F4), card Tracker mời SV vào cổng staff-only (F10) |
| (d) Phụ huynh / GV | /contact (1 click): hotline Zalo, email, 2 cơ sở đầy đủ | ✅ | Sang EN/JA vẫn gặp cả khối tiếng Việt cứng (F14) — đau nhất với đối tượng Nhật của một trường Việt-Nhật |

---

## 3. Findings

### 🔴 Nghiêm trọng (9)

**F1 · Mã ngành tuyển sinh 2026 sai — đúng trường hợp cảnh báo đỏ của đề bài.**
- `data/contact.ts:24` → `programCode: '7480204QTD'` là mã DUY NHẤT trong data; **không có 7480101 ở bất kỳ đâu trên /contact** (verify live: grep trang /contact chỉ ra `7480204QTD`, 0 lần `7480101`).
- Render tại 2 chỗ: khối "Tuyển sinh 2026" `app/contact/Content.tsx:74` và **ngay trang chủ** `components/home/CTASection.tsx:26` ("Mã ngành 7480204QTD … Chỉ tiêu 150 sinh viên (2026)").
- Kèm theo: `contact.ts:23` `programEN: '…(Honors Program)'` là danh xưng của khung 152 CLC — gắn vào khối tuyển sinh 2026 (khung 135 chuẩn 7480101) cũng lệch.
- **Hệ quả persona (a):** HS lớp 12 chép mã 7480204QTD vào phiếu đăng ký nguyện vọng 2026 là **sai mã**; phụ huynh đối chiếu với đề án tuyển sinh chính thức sẽ thấy cổng của chương trình ghi khác — mất tin ngay ở giao dịch quan trọng nhất của front door.
- **Sửa:** thêm trường mã theo khóa vào `contact.ts` (vd `programCode2026: '7480101'`, giữ `programCodeLegacy`), hiển thị "Mã ngành (từ 2026): **7480101** · khóa 2025 về trước: 7480204QTD" ở cả 2 chỗ render. Effort S.

**F2 · Card E-Service (sv04, thuần kế toán) LỘ trên trang chủ — trái quyết định 27/8.**
- `components/home/ServicesPortal.tsx:18`: `services.filter((s) => s.status === 'active').slice(0, 6)` — **chỉ lọc status, không lọc audience**. sv04 đứng thứ 5 trong mảng → chiếm 1/6 slot teaser. Verify live: chuỗi "Dịch vụ hành chính số" có trong HTML trang chủ.
- Trong khi `/services` đã lọc đúng (`app/services/Content.tsx:28` — `audience.includes('student')`) và chính `data/services.ts:318` ghi chú "KHONG phai dich vu SV BCSE (dinh chinh 27/8)".
- **Hệ quả:** SV bấm vào cổng kế toán; quyết định sản phẩm đã chốt bị chính trang chủ phá; teaser lãng phí 1 slot vàng.
- **Sửa:** thêm `.filter(s => s.audience.includes('student'))` — 1 dòng. Effort S.

**F3 · Card sv02 "Tư vấn AI — BCSE Advisor" mô tả một app không còn tồn tại ở URL đó.**
- `data/services.ts:240-262`: tên/mô tả/4 features đều là chatbot RAG. Thực tế sv02 đã là **BCSE Compass** (bản đồ hành trình + hồ sơ bảng điểm, SSO bắt buộc) — chính bcse-id đã đăng ký `sv02-compass: 'La bàn học tập…'` (`d:\files\bcse-id\src\data\apps.ts:82-92`); chatbot Advisor đang hold (Ollama tắt).
- Card này còn chiếm **slot 3/6 teaser trang chủ** (verify live "Tư vấn AI — BCSE Advisor" xuất hiện 2 lần: home + /services).
- **Hệ quả persona (b)/(c):** tân SV được hứa "chatbot tiếng Việt hỏi đáp" → đập vào màn hình login SSO của một app hoàn toàn khác; hai cổng của cùng hệ sinh thái (sv08 vs bcse-id) mô tả cùng một URL bằng hai sản phẩm khác nhau.
- **Sửa:** viết lại card theo Compass (đồng bộ chữ với apps.ts), journey `['all']` thay vì `['y1']`. Effort S.

**F4 · Card "BCSE Guild" trỏ sv15 — nhưng sv15 đang serve BCSE World.**
- `data/services.ts:590-628`: Guild active, url `sv15.bcse-vju.com`. Từ 27/8 sv15 là **BCSE World** (campus VJU 2D), Guild đã FREEZE trong git (EXECUTION-FALL-2026 B12.2 + handoff 25-27/8). Verify live: card Guild vẫn render trên /services.
- **Hệ quả:** SV bấm "Guild — quest board, XP, rank D→S" và rơi vào một thế giới 2D không có quest nào — đúng kiểu lỗi làm SV kết luận "portal khoa toàn đồ giả" (phản đề của toàn bộ STRATEGY P1).
- **Sửa:** hoặc đổi card thành "BCSE World" (mô tả thật), chuyển Guild về mục "Sắp trở lại"; hoặc gắn status coming-soon cho Guild. Effort S.

**F5 · Light mode vỡ hệ thống (dẫn chiếu audit riêng — không lặp lại).**
- Toàn bộ phân tích tại `docs/DESIGN-AUDIT-LIGHT-DARK-2026-08.md`: 8 lỗi NẶNG (chữ đen trên nền đen ở Hero/CTA/search/faculty…), root cause là blocklist `!important` ~60 dòng (`app/globals.css:232-312`) trong khi 0 component dùng `dark:`.
- **Bổ sung số đo mà audit chưa có** — accent không được override, đo trên nền light `#f8fafc`: badge "Đang hoạt động" emerald-400 = **1.84:1**; badge "Sắp ra mắt" amber-400 = **1.60:1**; tiêu đề nhóm dịch vụ violet-300/rose-300 = **1.76–1.81:1** (mù chữ theo đúng nghĩa đen); `text-slate-600→#94a3b8` = 2.45:1. Tức là ngoài 8 lỗi NẶNG, **mọi nhãn trạng thái và tiêu đề danh mục trên /services đều dưới 2:1 ở light mode**.
- **Sửa:** làm §5.2 (vá nhanh) của audit trước tuần định hướng; refactor token (§5.1) xếp lịch riêng. Effort M (vá) / L (token).

**F6 · Logo navbar 5.4 MB tải trên MỌI trang.**
- `components/layout/Navbar.tsx:36-40`: `<img src="/images/logos/vju.jpg">` hiển thị 56×56px; file thật `public/images/logos/vju.jpg` = **5,567,981 bytes (verify live HTTP 200)**. `next.config.mjs:4-6` `images.unoptimized` (bắt buộc với `output:'export'`) nên không có tầng tối ưu nào đỡ.
- **Hệ quả:** mọi first-visit (kể cả HS THPT trên 4G) tải 5.4 MB chỉ cho cái logo tròn; LCP/băng thông tệ đi vô nghĩa; đây gần chắc là asset nặng nhất toàn hệ sinh thái 20 cổng.
- **Sửa:** resize 112×112 + WebP (~5-10 KB), giữ nguyên markup. Effort S — quick win lớn nhất của cả review.

**F7 · Tương phản fail AA ngay ở DARK mode (theme chính).**
- Đo trên nền `#020617` (slate-950): `text-slate-500 #64748b` = **4.24:1** — fail AA cho text thường, mà đây là màu của HÀNG LOẠT mô tả cỡ 12px: mô tả card teaser (`ServicesPortal.tsx:56`), features card (`app/services/Content.tsx:422`), subtitle SectionTitle phụ, ghi chú curriculum. Trên nền card `slate-800` chỉ còn **3.07:1**.
- `text-slate-600 #475569` = **2.66:1** — dùng cho "+n môn khác…" (`CurriculumPreview.tsx:52`), đếm PC (`FacilitiesPreview.tsx:47`), copyright (`Footer.tsx:67`).
- Nút filter active `/services`: chữ trắng trên `bg-sky-500` = **2.77:1** (`app/services/Content.tsx:165`).
- Ngưỡng tham chiếu: slate-400 = 7.87:1 ✅ — tức chỉ cần nâng 1 nấc là thoát.
- **Hệ quả:** chính SV — người dùng mục tiêu — đọc mô tả dịch vụ khó ở cả 2 theme; vi phạm AA có hệ thống chứ không lẻ tẻ.
- **Sửa:** quy ước "text phụ tối thiểu slate-400 trên nền 950, slate-300 trên nền 800"; chip active đổi sky-600/700. Effort S–M (tìm-thay có kiểm soát).

**F8 · Số liệu "Cơ hội việc làm" không thể kiểm chứng + tự mâu thuẫn 3 chỗ.**
- `data/careers.ts:154-175` (render `CareerOpportunitiesSection.tsx:36-49`, verify live): "**>95% có việc trong 6 tháng sau tốt nghiệp**" — BCSE (mã thí điểm 7480204, tuyển từ ~2023) **chưa có khóa nào tốt nghiệp**; con số này không thể có nguồn từ chính chương trình. "100% SV năm 3 có thực tập", "30+ doanh nghiệp đối tác" cũng không dẫn nguồn.
- Tự mâu thuẫn: **30+ DN đối tác** (careers.ts:171) vs "**11 doanh nghiệp đối tác**" ngay trong card Career Portal (`data/services.ts:463`) và MASTER-PLAN N6; **13 ĐH đối tác Nhật** (`data/stats.ts:27`) vs "**7 trường đại học lớn tại Nhật Bản**" (`data/contact.ts:119` + `curriculum-135.ts:21` — hiển thị cùng lúc trên trang chủ và /contact).
- **Hệ quả persona (a)+(d):** một phụ huynh kỹ tính so 2 trang của CHÍNH site này đã bắt được 2 cặp số vênh nhau — toàn bộ số liệu còn lại (kể cả số thật) bị vạ lây. Với trường đại học, số bịa trên trang tuyển sinh là rủi ro uy tín cấp chương trình.
- **Sửa:** (người) thầy chốt nguồn từng số — số nào của VJU toàn trường thì ghi rõ "toàn VJU"; số chưa có thì bỏ hoặc đổi thành cam kết ("mục tiêu 100% thực tập"). Claude chỉ sửa data sau khi có nguồn. Effort S (kỹ thuật) + quyết định của thầy.

**F9 · Cửa "Đăng nhập" hiện là ngõ cụt với chính đối tượng vàng — tân SV K2026.**
- Nút login live từ 26/8 (đúng), nhưng theo memory 28/8 (`bcse-id cohort provisioning`): **K2026 = 0 tài khoản, chưa login được**. Tuần định hướng ~8/9 (D15) tân SV sẽ bấm nút này đầu tiên.
- sv08 cũng **không có bất kỳ khối nào cho tân SV**: không "Bắt đầu tại đây", không hướng dẫn "đăng nhập lần đầu bằng email VJU", journey map "Năm 1" bị giấu sau /services → view phụ (2 click + 1 toggle).
- **Hệ quả persona (b):** ấn tượng đầu tiên của khóa đầu tiên "sống trong hệ sinh thái từ ngày 1" (MASTER-PLAN N3) có nguy cơ là một màn hình từ chối đăng nhập.
- **Sửa:** phần seed thuộc bcse-id (ngoài phạm vi sv08 — việc phải làm trước 8/9); phần sv08: banner mùa nhập học "Tân SV K2026 → 3 bước bắt đầu" trỏ hướng dẫn login lần đầu. Effort S (sv08).

### 🟡 Trung bình (14)

**F10 · Card BCSE Tracker mời SV vào cổng staff-only.** `data/services.ts:423` `audience: ['student','faculty','staff']` → SV thấy card trên /services, nhưng dashboard bcse-id gate sv18 chỉ cho staff (`apps.ts:158`) và mô tả card tự nhận "cổng của **cố vấn**". SV bấm → chặn ở gate. Chọn 1: bỏ `student` khỏi audience, hoặc mô tả rõ "dữ liệu của bạn xem qua Compass".

**F11 · Card Koe ghi url sv20 nhưng sv20 đang serve Lambda Codex Campus LMS** (xác minh B10 24/8). `data/services.ts:874`. Hiện chưa lộ vì status coming-soon không render link (`Content.tsx:442-453`) — nhưng là quả mìn chờ ai đó flip status. Đồng thời **Lambda Codex card (services.ts:796) trỏ sv16** trong khi kế hoạch B12.1 gộp sv16→sv20; khi gộp xong nhớ đổi cả hai.

**F12 · Teaser trang chủ chọn app kiểu "6 phần tử active đầu mảng" chứ không phải chủ đích.** `ServicesPortal.tsx:14,18` — kết quả: sv10 In-3D đứng đầu, sv04+sv02 (lỗi F2/F3) chiếm 2 slot, còn **Code Arena, Career Portal, Thesis — 3 cổng Core có lực kéo thật — không có mặt**. Nút "Xem tất cả (20)" (`ServicesPortal.tsx:85`) đếm cả app ẩn khỏi SV trong khi /services chỉ hiện ~17 → hai con số không khớp nhau. Sửa: mảng `FEATURED_IDS` chủ đích (5 Core + Compass) + đếm theo danh sách student.

**F13 · /curriculum mặc định khung 152 giữa mùa tuyển sinh 2026.** `app/curriculum/Content.tsx:34` `useState<Framework>('152')` — là quyết định có chủ đích của thầy (D7 vòng 2, 26/8: "152 lên trước làm mặc định") nên KHÔNG tự ý đảo; nhưng nói thẳng: audience công khai lớn nhất của trang này lúc này là HS/PH khóa 2026 — họ phải tự hiểu và bấm toggle mới thấy chương trình mình sẽ học. Đề xuất mức tối thiểu không trái quyết định: đọc `?fw=135` từ URL + link "Bạn là thí sinh 2026? Xem khung 135" ngay trên toggle, và CTA tuyển sinh trỏ thẳng `/curriculum?fw=135`.

**F14 · i18n nửa vời — chuỗi Việt cứng lộ trong EN/JA.** Các chỗ đập vào mắt: khối "Điểm nổi bật" /contact hardcode VN (`app/contact/Content.tsx:112-119`), `admissionMethods`/`scholarshipNote` chỉ VN (`data/contact.ts:108-118`), eyebrow "TRIẾT LÝ ĐÀO TẠO" (`PhilosophySection.tsx:27`), "Xem tất cả dự án nghiên cứu"/"Khám phá…" (`StudentProjectsSection.tsx:87,95`), "Xem đội ngũ đầy đủ…" (`FacultyPreview.tsx:56`), "Khám phá cơ sở vật chất" (`FacilitiesPreview.tsx:85`), "+n môn khác..." (`CurriculumPreview.tsx:52`), toàn bộ `teachingMethods`+`internshipPartners` (`data/curriculum.ts:34-44`). Persona (d) người Nhật — đối tượng chiến lược của VJU — gặp trang "song ngữ" lốm đốm.

**F15 · EN/JA vô hình với máy tìm kiếm.** i18n là dict client-side (`lib/i18n.tsx:521-538`) trên static export → HTML tĩnh chỉ có tiếng Việt; không hreflang, không route /en /ja. Chấp nhận được ở giai đoạn này (user chính là VN) nhưng phải ghi nhận: mọi đầu tư nội dung EN/JA hiện **không có giá trị SEO nào** — đừng kỳ vọng tuyển sinh quốc tế tìm thấy qua Google.

**F16 · SEO cơ bản thiếu:** 0 thẻ OpenGraph/Twitter (grep live = 0) → share link lên Zalo/Facebook (kênh tuyển sinh chính!) không có ảnh/preview; `sitemap.xml` 404; description duy nhất ở layout gốc dùng chung mọi trang; `/research` thiếu cả title (F trong §2.1); StatCounter render "0" trong HTML tĩnh (`StatCounter.tsx:17` khởi tạo 0) → snippet Google có thể trích "0 tín chỉ".

**F17 · Subpage không có h1** — mọi trang con bắt đầu từ h2 (`SectionTitle.tsx:27`), xác minh live: 4 trang fetch có tổng cộng 1 h1 (trang chủ). Sửa: prop `as='h1'` cho SectionTitle đầu trang.

**F18 · Hero carousel không pause được trên touch** (`HeroSection.tsx:24-34`): auto 5.5s, chỉ pause khi hover (desktop) hoặc tab ẩn — WCAG 2.2.2 yêu cầu cơ chế dừng cho chuyển động >5s; mobile không có cách nào dừng. Kèm: không có `prefers-reduced-motion` cho gold-shine, live-cta aura, pulse, counter (toàn site, `globals.css:97-229`).

**F19 · Focus & ARIA:** buttons/toggles không có focus-visible ring chủ đích (dựa outline mặc định trên nền tối — khó thấy); hamburger thiếu `aria-expanded` (`Navbar.tsx:80-84`); LanguageToggle dropdown thiếu `aria-haspopup/expanded` (`LanguageToggle.tsx:30-37`); nút clear search aria-label="clear" tiếng Anh trong UI Việt (`services/Content.tsx:150`).

**F20 · `public/images/review/BCSE.docx` (1.3 MB) nằm công khai** — verify live tải được tại `/images/review/BCSE.docx`, 0 tham chiếu trong code. File .docx nội bộ trong thư mục public của front door = rò rỉ chờ được index. Gỡ khỏi `public/` (và khỏi `out/` trên server khi deploy lại).

**F21 · Asset chết ~1.8 MB:** `public/images/students/*` (5 file, có Picture16.png ~1MB), `research/*.jpg`, `specializations/*.png` — grep toàn code 0 tham chiếu; 4 thư mục rỗng (campus/faculty/hero/projects). Dọn hoặc dùng thật.

**F22 · 6 họ Google Fonts qua `@import` chặn render** (`globals.css:1`): Be Vietnam Pro + Fraunces + Inter + JetBrains Mono + Noto Sans JP + Orbitron; Geist local trong `app/fonts/` bỏ không. Identity cũng pha: Orbitron (sci-fi, cho số/logo) cạnh Fraunces (editorial serif, cho heading) là 2 giọng khác nhau trên cùng trang. Cắt còn Be Vietnam Pro + Fraunces (+ JetBrains Mono nếu tiếc), chuyển `next/font`.

**F23 · ThemeToggle nháy icon + ScrollReveal no-op** — đã ghi trong audit U1/U3, nhắc để gom việc: `ThemeToggle.tsx:7-12` khởi tạo cứng 'dark'; `ScrollReveal.tsx:11` nhận `delay` nhưng vứt đi, không có IntersectionObserver.

**F24 · Trang chủ vẫn dài (~34 màn hình mobile theo đo D13)** — đã rút 25/8 nhưng 12 section là nhiều cho một front door; đặc biệt persona (c) phải lướt qua 8 section brochure mới đến BCSE Portal. Không cần cắt ngay (quyết định thầy đang treo) nhưng thứ tự section nên cân nhắc đưa Portal lên sau Hero khi vào học kỳ (hero cho tuyển sinh, portal cho SV — 2 mùa 2 thứ tự).

### 🟢 Ghi nhận đúng/tốt (8) — chi tiết ở §6

F25 dữ liệu khung 135 chuẩn · F26 services audience model · F27 nút login đúng hướng B · F28 Umami · F29 i18n /research · F30 static export nhẹ · F31 contact thật · F32 dark identity.

---

## 4. Đề xuất LỆNH NÂNG CẤP (xếp theo ưu tiên)

> Nguyên tắc xếp: (1) sửa chỗ đang NÓI SAI trước (niềm tin là tài sản duy nhất của front door), (2) quick-win perf, (3) chuẩn bị tuần định hướng 8/9, (4) nợ hệ thống sau. Tôn trọng quy tắc "tối đa 2 hạng mục kỹ thuật cùng lúc" — I1–I3 gộp được thành 1 phiên "content truth pass".

| # | Lệnh | Mục tiêu | Việc cụ thể | Effort | Rủi ro |
|---|---|---|---|---|---|
| **I1** | **Sửa sự thật tuyển sinh 2026** | HS 2026 thấy đúng mã 7480101 ở mọi chỗ | `data/contact.ts`: thêm mã theo khóa; sửa render `contact/Content.tsx:74` + `CTASection.tsx:26` hiển thị cả 2 mã có chú thích khóa; rà lại nhãn "Honors Program" | S | Thấp — chỉ data+2 dòng render |
| **I2** | **Content truth pass danh mục dịch vụ** | Không còn card nói về app không tồn tại | (a) sv02 → BCSE Compass (đồng bộ chữ apps.ts); (b) Guild → "sắp trở lại", thêm card BCSE World nếu muốn công khai; (c) ghi chú Koe/Codex chờ B12.1; (d) Tracker bỏ audience student hoặc sửa mô tả | S | Thấp; cần thầy xác nhận công khai World hay chưa |
| **I3** | **Vá teaser trang chủ** | Trang chủ chỉ show app SV + Core | `ServicesPortal.tsx:18` thêm filter audience; thay `slice(0,6)` bằng `FEATURED_IDS` chủ đích (sv14, sv12, sv09, sv13, sv02-Compass, sv10); sửa số đếm nút "Xem tất cả" | S | Thấp |
| **I4** | **Logo + dọn public** | Bỏ 5.4MB/visit; hết file lạ công khai | Nén vju.jpg → WebP ≤20KB; gỡ `images/review/BCSE.docx`; xóa asset chết F21; redeploy để `out/` trên server sạch theo | S | Thấp — kiểm tra cache CF sau deploy |
| **I5** | **Số liệu có nguồn** | Không còn số không kiểm chứng trên trang tuyển sinh | 🧑‍🏫 thầy chốt: nguồn ">95% việc làm" (bỏ/ghi "toàn VJU"), 30+ vs 11 DN, 13 vs 7 ĐH Nhật; Claude sửa data 1 lượt | S (kỹ thuật) | Trung bình — cần quyết định người, đừng để Claude tự bịa số thay thế |
| **I6** | **Gói tân SV K2026 trên front door** | Tuần 8/9: tân SV bấm là vào được | Banner "Tân SV K2026 bắt đầu tại đây" (3 bước: email VJU → login id.bcse-vju.com → Compass); phối hợp việc bcse-id seed/kích hoạt K2026 (ngoài repo này, phải xong trước); CTA tuyển sinh → `/curriculum?fw=135` (F13) | S–M | Phụ thuộc bcse-id; banner theo mùa cần chỗ tắt/mở dễ |
| **I7** | **A11y contrast pass (dark)** | AA cho text thường ở theme chính | Nâng slate-500→400 (mô tả), slate-600→500 (phụ), chip active sky-600; thêm focus-visible ring, aria-expanded, pause carousel, `prefers-reduced-motion` media block | M | Thấp — thuần class; regression thị giác nhẹ, review bằng mắt 1 vòng |
| **I8** | **Light mode vá nhanh** | Hết "chữ đen nền đen" | Làm đúng §5.2 của `DESIGN-AUDIT-LIGHT-DARK-2026-08.md` + đổi accent -300/-400 → -600/-700 trong light cho badge/status (số đo F5) | M | Trung bình — blocklist dài thêm; chấp nhận vì là bản tạm |
| **I9** | **SEO pass** | Share được lên Zalo/FB, index đúng | metadata per-page (title+description), OG image tĩnh 1200×630, sitemap.xml + robots.txt vào `public/`, tách `/research` metadata (page server + Content client), h1 cho subpage (F17), StatCounter render giá trị thật khi no-JS | M | Thấp |
| **I10** | **Font diet + next/font** | FCP nhanh, identity gọn | Cắt còn 2–3 họ, bỏ `@import`, dùng `next/font/google` (hỗ trợ static export) | S–M | Trung bình — đổi metric font làm layout xê dịch nhẹ |
| **I11** | **Refactor token light/dark (đích 5.1 của audit)** | Hết nợ cấu trúc theme | Semantic CSS vars + Tailwind mapping, bỏ blocklist | L | Cao hơn — xếp SAU triage tháng 10, là "hạng mục kỹ thuật" riêng đúng nhịp MASTER-PLAN |

---

## 5. Điều KHÔNG nên làm

1. **Không refactor token theme ngay bây giờ** — tuần tới là tuần định hướng; vá nhanh (I8) đủ dùng, refactor L (I11) chen vào lúc này vi phạm chính quy tắc nhịp độ của MASTER-PLAN §3.
2. **Không biến sv08 thành app có đăng nhập/cá nhân hóa** — hướng B đã chốt và đang đúng (D8): sv08 tĩnh, hub cá nhân hóa ở bcse-id/Compass. Mọi ý tưởng "widget tuần này ngay trên sv08" là quay lại kiến trúc đã bác.
3. **Không đảo journey map thành view mặc định của /services** — thầy đã quyết 26/8 (D6): app tiện ích tự nói công dụng, journey là view phụ. Sửa nội dung card (I2) chứ không sửa lại IA đã duyệt.
4. **Không tự chế số liệu thay cho số bịa** — F8 sửa bằng nguồn thật từ thầy, không phải bằng con số "nghe hợp lý hơn".
5. **Không làm i18n route /en /ja SSG lúc này** — effort L, ROI thấp khi 95% người dùng đọc tiếng Việt; làm F14 (dọn chuỗi cứng) là đủ tử tế với người đọc EN/JA hiện có.
6. **Không thêm section/app mới vào trang chủ** — trang đã ~34 màn hình mobile; mọi bổ sung phải đổi bằng một section bị cắt.
7. **Không xóa app khỏi danh mục** khi sửa F3/F4 — nguyên tắc STRATEGY §3 "không xóa app, FREEZE = ngủ đông"; Guild/Advisor chuyển trạng thái "sắp trở lại", không biến mất.

---

## 6. Cái đang làm tốt

- **Dữ liệu khung 135 nghiêm túc và ĐÚNG** (`data/curriculum-135.ts`): 7 khối cộng đúng 135 TC; 23 TC cốt lõi cộng đúng từng học phần; 5 mô đun với 3 cửa ngõ đúng mã (CSE3069/3043/3042); 3 dự án HK5–7; 2 phương án tốt nghiệp kèm điều kiện GPA; kế hoạch 8 kỳ có mã học phần thật. Toggle 2 khung + ghi chú mã ngành theo khóa trên /curriculum là hình mẫu cho phần còn lại của site noi theo.
- **Mô hình audience + journey trong `data/services.ts`** đúng tinh thần N1: gán tay 20 app, E-Office ẩn đúng, journey 5 chặng có lời dẫn 3 thứ tiếng viết tử tế.
- **Nút Đăng nhập theo hướng B** — sv08 giữ tĩnh, đẩy về id.bcse-vju.com; kiến trúc phân vai sạch, verify live cả desktop lẫn mobile menu.
- **Umami gắn đúng** (`app/layout.tsx:31-36`) — front door có đo lường, đúng GĐ0.
- **/research là trang i18n tốt nhất site** — copy EN/JA viết riêng từng lab/area/blog, không máy móc.
- **Ảnh facilities nén tốt** (16–252 KB) và static export + CF Tunnel cho một trang brochure là lựa chọn kỹ thuật đúng — rẻ, nhanh, không bề mặt tấn công.
- **Thông tin liên hệ thật và đầy đủ** — hotline Zalo, email, 2 cơ sở, tổ hợp xét tuyển: persona (d) được phục vụ chu đáo.
- **Dark identity nhất quán** — slate + sky/amber, SectionTitle serif Fraunces, hero cinematic có nghề; vấn đề của thị giác nằm ở light mode và contrast, không nằm ở gu.
